import { Routes } from '@angular/router';
import { TimeNavigationComponent } from './components/time-navigation/time-navigation.component';

export const routes: Routes = [
  { path: '', component: TimeNavigationComponent },
];
